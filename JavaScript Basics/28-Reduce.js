const number = [1, 2, 3, 4];
const rs = number.reduce((p,c)=>{
    return p + c;
},0)

console.log(rs);


const person = [
    {name: 'John', age:24},
    {name: 'Mary', age:25},
    {name: 'Peter', age:26}
]

person.reduce((p,c)=>{
    
});